apiVersion: v1
kind: Service
metadata:
  name: orion
  labels:
    app.kubernetes.io/name: orion
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: orion
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
